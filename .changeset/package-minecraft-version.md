---
"@lpsmods/mc-build": patch
---

Read package versions from the project directory and strip prerelease labels and build metadata for Minecraft. Report missing or invalid versions instead of silently using a fallback.
